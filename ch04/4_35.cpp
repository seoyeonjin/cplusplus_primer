// a. 'a'가 int로 변환되어 계산되어 char 변수에 저장된다. 
// b. f = ui - ival * 1.0 -> int * 1.0 double로 변환. unsigned int - double -> double. f에 저장할 때 float로
// c. d = ui * f -> ui가 f로 변환. double 로 저장.
// d. c = i + f + d -> i가 f로 변환. i,f가 d로 변환. 저장될 때 c로 변환
